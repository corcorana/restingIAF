\documentclass{article}

\usepackage[a4paper,margin=2cm,portrait]{geometry}
\usepackage[latin1]{inputenc}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows}
\begin{document}
\thispagestyle{empty}


% Define block styles
\tikzstyle{decision} = [diamond, draw, fill=yellow!20, 
    text width=5em, text badly centered, node distance=2.5cm, inner sep=1pt]
\tikzstyle{block} = [rectangle, draw, fill=blue!20, 
    text width=7.5em, text centered, rounded corners, minimum height=4em]
\tikzstyle{line} = [draw, -latex']
\tikzstyle{cloud} = [ellipse, draw, fill=red!20, node distance=2cm,
    minimum height=4em]
\tikzstyle{ball} = [circle, draw, fill=red!20, 
    text width=5em, text centered, node distance=3cm]    

\begin{tikzpicture}[node distance = 2cm, auto]
    % Place nodes
    \node [cloud] (raw) {\bf{Channel data}};
    \node [block, below of=raw] (sgf) {\bf{Calculate $PSD$ \& derivatives}};
    \node [block, below of=sgf] (win) {\bf{Search for zero-crossings}};
    \node [decision, below of=win] (dgzc) {\bf{Crossing detected?}};
    \node [ball, right of=dgzc] (noPaf) {\bf{Exclude channel}};
    \node [decision, below of=dgzc, yshift=-0.6cm] (ax) {\bf{Satisfies criteria?}};
    \node [ball, right of=ax] (noCog) {\bf{Exclude channel}};
    \node [block, below of=ax, yshift=-0.5cm] (ests) {\bf{Calculate IAF estimates}};

    % Draw edges
    \path [line] (raw) -- (sgf);
    \path [line] (sgf) -- (win);
    \path [line] (win) -- (dgzc);
    \path [line] (dgzc) -- node {\bf{no}} (noPaf);
    \path [line] (dgzc) -- node {\bf{yes}} (ax);
    \path [line] (ax) -- node {\bf{no}} (noCog);
    \path [line] (ax) -- node {\bf{yes}} (ests);

\end{tikzpicture}

\end{document}